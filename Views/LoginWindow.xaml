<Window x:Class="TaskFlowStudent.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Width="1000" Height="650"
        WindowStartupLocation="CenterScreen"
        Background="Black">

    <Grid>
        <!-- Fundal gradient -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#1E1E2F" Offset="0.0" />
                <GradientStop Color="#0F0F1A" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Card -->
        <Border Background="#1C1C2E"
                CornerRadius="15"
                Padding="30"
                Width="300"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>

                <!-- Titlu -->
                <TextBlock Text="Autentificare"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 20"/>

                <!-- Username -->
                <TextBlock Text="Nume utilizator" Foreground="White" Margin="0 0 0 5"/>
                <TextBox x:Name="UsernameBox"
                         Height="30"
                         FontSize="14"
                         Margin="0 0 0 15"
                         Padding="5"
                         Background="#2A2A3D"
                         Foreground="White"
                         BorderThickness="0"/>

                <!-- Password -->
                <TextBlock Text="Parolă" Foreground="White" Margin="0 0 0 5"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="30"
                             FontSize="14"
                             Margin="0 0 0 25"
                             Padding="5"
                             Background="#2A2A3D"
                             Foreground="White"
                             BorderThickness="0"/>

                <!-- Buton Login -->
                <Button Content="Login"
                        Height="35"
                        Background="#00C853"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        FontSize="14"
                        Cursor="Hand"
                        Click="Login_Click"
                        Margin="0 10 0 0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
